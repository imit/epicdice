<h1>Listing characters</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @player_characters.each do |player_character| %>
  <tr>
    <td><%= link_to  player_character.name, player_character %></td>
<td><%= link_to  player_character.character_class_name, player_character %></td>
    <td><%= link_to 'Edit', edit_player_character_path(player_character) %></td>
    <td><%= link_to 'Destroy', player_character, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Character', new_player_character_path %>
